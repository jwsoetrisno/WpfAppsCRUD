<Window x:Class="WpfApps.Views.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"        
        xmlns:local="clr-namespace:WpfApps"
        xmlns:init="http://schemas.microsoft.com/xaml/behaviors"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <DataGrid ItemsSource="{Binding Products}" SelectedItem="{Binding SelectedProduct}" AutoGenerateColumns="False" Grid.Row="0">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Id" Binding="{Binding Id}" IsReadOnly="True"/>
                <DataGridTextColumn Header="Name" Binding="{Binding Name}"/>
                <DataGridTextColumn Header="SKU" Binding="{Binding SKU}"/>
                <DataGridTextColumn Header="Price" Binding="{Binding Price}"/>
                <DataGridTextColumn Header="Stock" Binding="{Binding StockQuantity}"/>
            </DataGrid.Columns>
        </DataGrid>

        <StackPanel Orientation="Horizontal" Grid.Row="1" HorizontalAlignment="Center" Margin="0,10,0,0">
            <Button Content="Refresh" Command="{Binding LoadCommand}" Width="75" Margin="5"/>
            <Button Content="Add" Command="{Binding AddCommand}" Width="75" Margin="5"/>
            <Button Content="Update" Command="{Binding UpdateCommand}" Width="75" Margin="5"/>
            <Button Content="Delete" Command="{Binding DeleteCommand}" Width="75" Margin="5"/>
            <Button Content="Export" Command="{Binding ExportCommand}" Width="75" Margin="5"/>
            <Button Content="Import" Command="{Binding ImportCommand}" Width="75" Margin="5"/>

            <init:Interaction.Triggers>
                <init:EventTrigger EventName="Loaded">
                    <init:InvokeCommandAction Command="{Binding LoadCommand}"/>
                </init:EventTrigger>
            </init:Interaction.Triggers>
            
        </StackPanel>
    </Grid>
</Window>
